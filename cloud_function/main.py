def handler(request=None):
    """Simple Cloud Function entry point.

    Adapt this `handler` function to your cloud provider's expected signature.
    For local testing you can call `python -m cloud_function.main`.
    """
    return "OK"


if __name__ == "__main__":
    print("cloud_function: simple handler -> ready")
